[inet_http_server]
port = *:9001
username = username
password = password

[supervisord]
logfile = /mnt/logs/supervisord.log
logfile_maxbytes = 50MB
logfile_backups = 10
loglevel = debug
pidfile = /mnt/config/supervisord.pid
nodaemon = true
minfds = 1024
minprocs = 200
environment = PPP_CONFIG_DIR="/mnt/config/"
; Comment the above environment setting and uncomment the following
; line if you have compressed files.  Place xRITDecompress - available
; from Eumetsat - program to the configuration directory
; environment = PPP_CONFIG_DIR="/mnt/config/",XRIT_DECOMPRESS_PATH="/mnt/config/xRITDecompress

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl = http://127.0.0.1:9001

[program:nameserver]
command = /opt/conda/bin/nameserver
redirect_stderr = true
priority = 1
stdout_logfile = /mnt/logs/supervisor_nameserver_msg0deg.log
startsecs = 10
stopsignal = KILL

[program:trollstalker]
command = /opt/conda/bin/python /opt/conda/bin/trollstalker.py -c /mnt/config/trollstalker.ini -C msg_0_deg
redirect_stderr = true
stdout_logfile = /mnt/logs/supervisor_trollstalker-msg.log
stopsignal = KILL

[program:trollflow2]
command = /opt/conda/bin/python /opt/conda/bin/satpy_launcher.py /dataset/msg0deg /mnt/config/trollflow2_seviri_hrit.yaml
redirect_stderr = true
stdout_logfile = /mnt/logs/supervisor_trollflow_msg0deg.log
stopsignal = KILL

[program:segment_gatherer]
command = /opt/conda/bin/python /opt/conda/bin/segment_gatherer.py -c /mnt/config/segment_gatherer.yaml -v -l /mnt/logs/segment_gatherer_msg0deg.log
redirect_stderr = true
stdout_logfile = /mnt/logs/supervisor_segment_gatherer_msg0deg.log
stopsignal = KILL               ; signal used to kill process (default TERM)

;[eventlistener:crashmailbatch]
;command = /opt/conda/bin/crashmailbatch --toEmail="someone@somewhere.com" --fromEmail="supervisord@msg0deg.test.com" -s "Crash alert from supervisord"
;events = PROCESS_STATE,TICK_60

;[eventlistener:fatalmailbatch]
;command = /opt/conda/bin/fatalmailbatch --toEmail="someone@somewhere.com" --fromEmail="supervisord@msg0deg.test.com" -s "Fatal start alert from supervisord"
;events = PROCESS_STATE,TICK_60
